info:
  name: Create
  type: http
  seq: 3

http:
  method: POST
  url: 127.0.0.1:8090//api/collections/Activity/records
  body:
    type: multipart-form
    data:
      - name: title
        type: text
        value: Golf
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
